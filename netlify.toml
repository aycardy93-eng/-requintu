[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Single Page App: cualquier ruta sirve index.html y React maneja el routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
