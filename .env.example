PORT=3000

# Obligatorio: cadena aleatoria larga, p. ej. `openssl rand -hex 32`
JWT_SECRET=

# URL pública del backend, usada para construir los enlaces de las imágenes subidas
PUBLIC_URL=http://localhost:3000

# Orígenes autorizados para CORS, separados por coma
ALLOWED_ORIGINS=http://localhost:5173

DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=requintu_db
